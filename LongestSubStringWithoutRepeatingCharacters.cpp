#include<iostream>
#include<string>
using namespace std;

int main()
{
	string a = "dvdf";
	int startIndex = 0;
	int endIndex;
	string temp;

	for (int i = 0; i < a.length(); i++)
	{
		if(temp.find(a[i]) != string::npos)
	}
	
	return 0;
}